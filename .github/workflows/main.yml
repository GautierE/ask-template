name: GitHub Actions Demo

on: 
  push:
    branches: [main]

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: cd front
      - run: npm install
      - run: eslint .
